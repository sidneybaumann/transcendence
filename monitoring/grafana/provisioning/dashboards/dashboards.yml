apiVersion: 1

providers:
  - name: 'Dashboards' # A name for this provider
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json # Where JSONs live inside the container